use crate::bot::*;
use crate::map::*;

pub fn solve(map: &mut Map) -> Vec<Action> {
    let mut action_list = Vec::new();
    while (!map.is_complete()) {
        //try and make it more complete
    }
    return action_list;
}
